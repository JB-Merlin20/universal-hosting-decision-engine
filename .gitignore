# Dependencies
node_modules/
# Ignore app lockfile (app/package-lock.json) but track root lockfile
app/package-lock.json

# Next.js build output
app/.next/
.next/

# Production
app/out/

# Debug
npm-debug.log*

# Environment
.env
.env.local
.env.production.local

# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Agent snapshots (regenerated)
data/snapshots/

# Generated files (regenerated by agent pipeline — tracked for CI daily updates)
# data/hosting-processed.json  — tracked for daily price workflow
# data/hosting-export.csv      — tracked for daily price workflow
# data/scrape-report.json      — tracked for consecutive failure detection
# Note: app/src/data/hosting-data.json must be tracked for Vercel builds
